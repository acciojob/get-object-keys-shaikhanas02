//your JS code here. If required.
//your JS code here. If required.
let student = {
	name: `Anas`,
	job: `SDE`,
	experience: `fresher`

	getKeys(student);
}
function getKeys(obj) {
let keys = Object.keys(obj)
return keys;
	
}

student.getKeys;
